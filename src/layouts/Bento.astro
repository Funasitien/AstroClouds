---
const {
  title,
  description,
  image_url = "#",
} = Astro.props;
---

<div class="min-h-screen grid grid-cols-2 lg:grid-cols-6">
  	<div class="col-span-2 ml-8 mt-12">
      <img src={ image_url } class="rounded-full border-1" alt="C'est mon avatar ça astro.">
    	<h1 class="text-4xl py-4 font-bold">{ title }</h1>
      <p>{ description }</p>
  	</div>
	  <div class="col-span-2 lg:col-span-4 grid grid-cols-4 lg:grid-cols-8 gap-4" style="grid-auto-rows: 13%;">
      <slot />
    </div>
</div>
